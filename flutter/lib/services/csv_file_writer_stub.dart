import 'csv_file_write_result.dart';

Future<CsvFileWriteResult> writeCsvFile({
  required String metricName,
  required String csvContent,
}) {
  throw UnsupportedError('CSV 파일 저장은 모바일 또는 데스크톱 실행에서 사용할 수 있습니다.');
}
